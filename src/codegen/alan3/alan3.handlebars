{{#each map.rooms}}
The {{{className this.name}}} Isa location
  Name '{{{this.name}}}'
  {{#if this.description}}
  Description
    "{{{this.description}}}"
  {{/if}}
  {{#each this.connections}}
  Exit {{ dirToStr this.startDir }} To {{{className this.room.name}}}.
  {{/each}}
End The {{{className this.name}}}.

{{#each this.objects}}
{{> alan3Object object=this location=../this}}
{{/each}}

{{/each}} 

{{#if map.startRoom}}Start at {{{className map.startRoom.name}}}.
{{/if}}
